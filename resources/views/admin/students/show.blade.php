{{-- resources/views/admin/students/show.blade.php --}} <x-app-layout> <x-slot name="header"> <div class="flex items-center justify-between"> <h2 class="text-xl font-semibold">{{ $student->name }}</h2> <a href="{{ route('admin.students.edit', $student) }}" class="px-4 py-2 bg-yellow-500 text-white rounded-md text-sm">Edit</a> </div> </x-slot> <div class="py-8 max-w-4xl mx-auto px-4 space-y-6"> <div class="bg-white rounded-lg shadow p-6 grid grid-cols-2 gap-4"> <div> <p class="text-xs text-gray-500 uppercase">Student number</p> <p class="font-medium">{{ $student->studentProfile?->student_number ?? '—' }}</p> </div> <div> <p class="text-xs text-gray-500 uppercase">Email</p> <p class="font-medium">{{ $student->email }}</p> </div> <div> <p class="text-xs text-gray-500 uppercase">Phone</p> <p class="font-medium">{{ $student->studentProfile?->phone ?? '—' }}</p> </div> <div> <p class="text-xs text-gray-500 uppercase">Date of birth</p> <p class="font-medium">{{ $student->studentProfile?->date_of_birth?->format('d M Y') ?? '—' }}</p> </div> <div class="col-span-2"> <p class="text-xs text-gray-500 uppercase">Address</p> <p class="font-medium">{{ $student->studentProfile?->address ?? '—' }}</p> </div> <div> <p class="text-xs text-gray-500 uppercase">Status</p> <x-badge :status="$student->studentProfile?->status ?? 'active'"/> </div> <div> <p class="text-xs text-gray-500 uppercase">Joined</p> <p class="font-medium">{{ $student->created_at->format('d M Y') }}</p> </div> </div> <div class="bg-white rounded-lg shadow overflow-hidden"> <div class="px-6 py-4 border-b border-gray-200 font-medium">Enrolments</div> <table class="min-w-full divide-y divide-gray-200"> <thead class="bg-gray-50"> <tr> <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Course</th> <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Status</th> <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase">Enrolled</th> </tr> </thead> <tbody class="divide-y divide-gray-200"> @forelse($student->enrolments as $enrolment) <tr> <td class="px-6 py-4 text-sm font-medium">{{ $enrolment->course->title }}</td> <td class="px-6 py-4"><x-badge :status="$enrolment->status"/></td> <td class="px-6 py-4 text-sm text-gray-500"> {{ $enrolment->enrolled_at?->format('d M Y') ?? '—' }} </td> </tr> @empty <tr> <td colspan="3" class="px-6 py-4 text-sm text-gray-500">Not enrolled in any courses.</td> </tr> @endforelse </tbody> </table> </div> </div> </x-app-layout>